For getting all interactions 

End Points

localhost:8000/api/user/register
Fileds : name, email, password are required parameters

localhost:8000/api/user/login
email, password are required parameters

localhost/api/user/get/interactions
Bearer token is required

localhost:8000/api/user/create/interaction
name, description are required fields
Bearer token is required after login



localhost:8000/api/user/update/interaction/1
name, description are required fields
Bearer token is required after login

localhost:8000/api/user/delete/interaction/2
Bearer token is required after login and id you want to delete

localhost:8000/api/user/logout
Bearer token is required